import 'package:flutter/material.dart';
    
class AuthModulePage extends StatelessWidget {
  const AuthModulePage({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('AuthModule page')),
    );
  }
}
